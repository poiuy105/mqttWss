# Home Assistant Integration - Verification Checklist

* [ ] Home Assistant configuration container is visible between Protocol and Features containers on Setting page

* [ ] HA address input field is present with default value "homeassistant.local"

* [ ] Long-lived token input field is present with default value "abcdefg"

* [ ] Language input field is present with default value "zh"

* [ ] HTTPS checkbox is present and checked by default

* [ ] Home Assistant settings are saved when app is closed and reopened

* [ ] "Send to Home Assistant" checkbox is present under prefix container on Voice page

* [ ] Only Capture Frame and Tap App Name containers have reduced height

* [ ] When "Send to Home Assistant" is enabled, captured voice commands are sent to Home Assistant

* [ ] Home Assistant responses are displayed in a popup

* [ ] Home Assistant response speech is broadcast via TTS

* [ ] Error handling works for failed Home Assistant requests

* [ ] All existing functionality continues to work

